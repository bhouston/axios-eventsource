{
  "name": "example-fastify-server",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/server.ts",
    "start": "node dist/server.js",
    "test": "echo \"No tests yet\""
  },
  "dependencies": {
    "fastify": "^5.6.1",
    "fastify-file-router": "^2.6.1"
  },
  "devDependencies": {
    "tsx": "^4.20.5"
  }
}
